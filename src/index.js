import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';


ReactDOM.render( <
    React.StrictMode >
    <
    span > < /span> <
    App / >
    <
    /React.StrictMode>,
    document.getElementById('root')
);